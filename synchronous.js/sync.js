// JavaScript is a single-threaded, concurrent programming language that is synchronous by default. 
// This means that each line of code is executed sequentially, and one task must finish before the next can start. 

// Synchronous JavaScript / single threaded language - default behaviour of js is synchronous it is not asynchronous. the features of 
// asynchronous feature of js is provided by browser it is not provided by js. 

// synchronous / single thread - synchronous means that each line of code is executed sequentially, and one task must finish before 
// the next can start.